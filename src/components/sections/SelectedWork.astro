---
/**
 * Selected Work (homepage teaser), 3 cards, deep-linked to /work.
 *
 * Spec ref: docs/design/S6_PLUS.md Section 5 (~line 626).
 * S7+ update (2026-05-12): StainSlayer + Demiurge+MyKonos cards have
 * real hero images. Sogn keeps the editorial glyph (mark + 2 heroes mix).
 * Demiurge + MyKonos merges engine + product into one card per Preston.
 * Locked S7+ (2026-05-12).
 */

interface WorkCard {
  id: string;
  title: string;
  desc: string;
  stack: [string, string];
  status: 'LIVE' | 'BETA' | 'PROD';
  built_in_days?: number;
  external_url?: string;
  /** False keeps the card off the homepage until the site's own domain is live. */
  published?: boolean;
  /** Optional hero image (overrides editorial glyph). */
  image?: {
    src: string;
    srcSmall?: string;
    srcXSmall?: string;
    alt: string;
    width?: number;
    height?: number;
  };
}

const CARDS: WorkCard[] = [
  {
    id: 'us-survey-supply',
    title: 'US Survey Supply',
    desc: 'Site for a Bandon surveying-equipment dealer shipping nationwide.',
    stack: ['Astro', 'Cloudflare'],
    status: 'LIVE',
    // Flip to true at the ussurveysupply.com cutover (tracks/wc-taylor-cutover.md W9).
    published: false,
    external_url: 'https://ussurveysupply.com',
    image: {
      src: '/work/taylor-card.webp',
      srcSmall: '/work/taylor-card-720.webp',
      srcXSmall: '/work/taylor-card-480.webp',
      alt: 'US Survey Supply, a Carlson Centric Dealer in Bandon, Oregon: white wordmark and red-and-white survey target on navy beside the Coquille River Lighthouse.',
      width: 1200,
      height: 630,
    },
  },
  {
    id: 'bandon-by-the-dunes',
    title: 'Bandon By The Dunes Realtee',
    desc: 'Real estate site with listings live from the RMLS feed.',
    stack: ['Astro', 'RMLS API'],
    status: 'LIVE',
    built_in_days: 18,
    external_url: 'https://www.bandonbythedunesrealtee.net',
    image: {
      src: '/work/bbtd-card.webp',
      srcSmall: '/work/bbtd-card-720.webp',
      srcXSmall: '/work/bbtd-card-480.webp',
      alt: 'Bandon By The Dunes Realtee: navy lighthouse mark on cream.',
      width: 1200,
      height: 630,
    },
  },
  {
    id: 'seabreeze',
    title: 'SeaBreeze Landscape & Home Repair',
    desc: 'Site for a Bandon landscape-care and home-repair business.',
    stack: ['Astro', 'Cloudflare'],
    status: 'LIVE',
    built_in_days: 7,
    external_url: 'https://seabreeze.llc',
    image: {
      src: '/work/seabreeze-card.webp',
      srcSmall: '/work/seabreeze-card-720.webp',
      srcXSmall: '/work/seabreeze-card-480.webp',
      alt: 'SeaBreeze Landscape and Home Repair, Bandon Oregon: coastal logo in cream and teal on a deep green field.',
      width: 1200,
      height: 630,
    },
  },
  {
    id: 'sogn-contracting',
    title: 'Sogn Contracting',
    desc: 'Marketing site for a 30-year Bandon contractor.',
    stack: ['Next.js', 'Vercel'],
    status: 'LIVE',
    built_in_days: 3,
    external_url: 'https://sogncontracting.com',
    image: {
      src: '/work/sogn-card.webp',
      srcSmall: '/work/sogn-card-720.webp',
      srcXSmall: '/work/sogn-card-480.webp',
      alt: 'Sogn Contracting LLC, Bandon Oregon. 30 years of construction. Paul Sogn, Oregon CCB# 257491.',
      width: 1200,
      height: 630,
    },
  },
  {
    id: 'stainslayer',
    title: 'StainSlayer AI',
    desc: 'Vision AI stain analysis. App Store + Play Store.',
    stack: ['Flutter', 'Supabase'],
    status: 'LIVE',
    built_in_days: 30,
    image: {
      src: '/work/stainslayer-card.webp',
      srcSmall: '/work/stainslayer-card-720.webp',
      srcXSmall: '/work/stainslayer-card-480.webp',
      alt: 'StainSlayer AI: rainbow stain splash with script tagline.',
      width: 1280,
      height: 625,
    },
  },
  {
    id: 'demiurge-mykonos',
    title: 'Demiurge + MyKonos',
    desc: 'Memory engine powering the MyKonos AI companion.',
    stack: ['Demiurge', 'Flutter'],
    status: 'BETA',
    built_in_days: 14,
    image: {
      src: '/work/demiurge-card.webp',
      srcSmall: '/work/demiurge-card-720.webp',
      srcXSmall: '/work/demiurge-card-480.webp',
      alt: 'Project Demiurge: hexagonal node graph in teal with one node lit orange.',
      width: 1280,
      height: 640,
    },
  },
];
---

<section class="selected" id="work" aria-labelledby="selected-h">
  <header class="selected-head">
    <div class="selected-eyebrow">
      <span class="line" aria-hidden="true"></span>
      <h2 class="selected-h" id="selected-h">02 · SELECTED WORK</h2>
      <span class="selected-status">built and live</span>
    </div>
    <a href="/work" class="selected-link">see all →</a>
  </header>

  <ul class="selected-grid" role="list">
    {CARDS.filter((card) => card.published !== false).map((card) => (
      <li class="sw-card-wrap" role="listitem">
        <a
          href={card.external_url ?? `/work#${card.id}`}
          target={card.external_url ? '_blank' : undefined}
          rel={card.external_url ? 'noopener' : undefined}
          class="sw-card"
          data-slug={card.id}
        >
          <div class="sw-preview" aria-hidden={card.image ? undefined : 'true'}>
            {card.image ? (
              <picture>
                {card.image.srcSmall && (
                  <source
                    type="image/webp"
                    srcset={`${card.image.srcXSmall ? card.image.srcXSmall + " 480w, " : ""}${card.image.srcSmall} 720w, ${card.image.src} 1280w`}
                    sizes="(min-width: 1024px) 320px, (min-width: 740px) 50vw, calc(100vw - 8px)"
                  />
                )}
                <img
                  src={card.image.src}
                  alt={card.image.alt}
                  width={card.image.width ?? 1280}
                  height={card.image.height ?? 640}
                  loading="lazy"
                  decoding="async"
                  class="sw-img"
                />
              </picture>
            ) : (
              <span class="sw-mark">{card.title.charAt(0)}</span>
            )}
          </div>
          <div class="sw-meta">
            <div class="sw-title-row">
              <span class="sw-title">{card.title}</span>
              <span class={`sw-status sw-status-${card.status.toLowerCase()}`}>{card.status}</span>
            </div>
            <p class="sw-desc">{card.desc}</p>
            <div class="sw-stack">
              {card.stack.map((s) => <span class="sw-tag">{s}</span>)}
              {card.built_in_days != null && <span class="sw-built">Built in {card.built_in_days}d</span>}
            </div>
          </div>
        </a>
      </li>
    ))}
  </ul>
</section>

<style>
  .selected {
    padding: 40px 4px 48px;
    max-width: 720px;
    margin: 0 auto;
  }
  @media (min-width: 740px) {
    .selected { padding: 56px 4px 64px; max-width: 860px; }
  }
  @media (min-width: 1024px) {
    .selected { padding: 72px 0 80px; max-width: 960px; }
  }

  .selected-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    flex-wrap: wrap;
    padding-bottom: 16px;
    border-bottom: 1px solid var(--copper-hairline);
    margin-bottom: 20px;
  }
  .selected-eyebrow {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .selected-eyebrow .line {
    width: 28px;
    height: 1px;
    background: var(--color-copper);
  }
  @media (min-width: 1024px) { .selected-eyebrow .line { width: 40px; } }
  .selected-h {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.22em;
    color: var(--color-copper);
    text-transform: uppercase;
    margin: 0;
  }
  @media (min-width: 1024px) { .selected-h { font-size: 13px; } }
  .selected-status {
    font-family: var(--font-sans);
    font-size: 11px;
    color: var(--color-ash-500);
    font-style: italic;
  }
  .selected-link {
    font-family: var(--font-sans);
    font-size: 12px;
    color: var(--color-ash-400);
    transition: color 200ms;
  }
  .selected-link:hover { color: var(--color-wc-green); }

  /* ─── Grid ─── */
  .selected-grid {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 16px;
  }
  @media (min-width: 740px) {
    .selected-grid {
      grid-template-columns: 1fr 1fr;
      gap: 20px;
    }
    .sw-card-wrap:first-child { grid-column: span 2; }
  }
  @media (min-width: 1024px) {
    .selected-grid {
      grid-template-columns: 1fr 1fr 1fr;
      gap: 24px;
    }
    .sw-card-wrap:first-child { grid-column: auto; }
  }

  /* ─── Card ─── */
  .sw-card {
    display: grid;
    grid-template-rows: 140px 1fr;
    background: var(--color-bg-card);
    border: 1px solid var(--copper-hairline);
    border-radius: var(--radius-card);
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    transition: border-color 200ms, transform 200ms, box-shadow 200ms;
    min-height: 280px;
  }
  @media (min-width: 1024px) {
    .sw-card { grid-template-rows: 160px 1fr; min-height: 320px; }
  }
  .sw-card:hover,
  .sw-card:focus-visible {
    border-color: var(--color-wc-green);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4), 0 0 0 1px var(--color-wc-green);
    outline: none;
  }

  .sw-preview {
    background:
      radial-gradient(circle at 30% 40%, rgba(216, 144, 90, 0.15), transparent 60%),
      linear-gradient(135deg, var(--color-bg-raised), var(--color-bg-card));
    display: flex;
    align-items: center;
    justify-content: center;
    border-bottom: 1px solid var(--copper-hairline);
    position: relative;
  }
  .sw-preview > picture,
  .sw-img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }
  /* Sogn card image fills the slot via the universal cover above.
     If logo cropping is an issue, the answer is a different image
     (e.g. real Sogn site screenshot), not a contain override that
     creates dead whitespace in the card slot. */
  .sw-preview > picture {
    line-height: 0;
  }
  .sw-mark {
    font-family: var(--font-serif);
    font-size: 64px;
    font-weight: 500;
    color: var(--color-copper);
    font-style: italic;
    line-height: 1;
    opacity: 0.7;
  }

  .sw-meta {
    padding: 14px 16px 18px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .sw-title-row {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 8px;
  }
  .sw-title {
    font-family: var(--font-serif);
    font-size: 17px;
    font-weight: 600;
    color: var(--color-ash-100);
    letter-spacing: -0.01em;
    min-width: 0;
  }
  @media (min-width: 1024px) {
    .sw-title { font-size: 19px; }
  }
  .sw-status {
    font-family: var(--font-mono);
    font-size: 9px;
    letter-spacing: 0.1em;
    padding: 2px 6px;
    border-radius: 3px;
    text-transform: uppercase;
    font-weight: 600;
    white-space: nowrap;
    flex-shrink: 0;
  }
  .sw-status-live {
    background: rgba(77, 216, 168, 0.15);
    color: var(--color-wc-green);
    border: 1px solid rgba(77, 216, 168, 0.3);
  }
  .sw-status-beta {
    background: rgba(240, 184, 120, 0.15);
    color: var(--color-amber);
    border: 1px solid rgba(240, 184, 120, 0.3);
  }
  .sw-status-prod {
    background: rgba(216, 144, 90, 0.15);
    color: var(--color-copper-bright);
    border: 1px solid var(--copper-hairline);
  }

  .sw-desc {
    font-family: var(--font-sans);
    font-size: 13px;
    color: var(--color-ash-400);
    line-height: 1.5;
    margin: 0;
  }
  @media (min-width: 1024px) {
    .sw-desc { font-size: 14px; }
  }

  .sw-stack {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 4px;
    align-items: center;
  }
  .sw-tag {
    font-family: var(--font-mono);
    font-size: 9px;
    color: var(--color-ash-500);
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.06);
    padding: 2px 6px;
    border-radius: 3px;
    letter-spacing: 0.04em;
  }
  .sw-built {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: 9px;
    color: var(--color-copper);
    letter-spacing: 0.06em;
  }
</style>
